using APIVENDASCORE.Models;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography.X509Certificates;

namespace APIVENDASCORE.Repositorys
{

    namespace APIVENDASCORE.Repositories
    {
        public class UserRepository
        {
            //public static Usuario Get(string Username, string Password)
            //{

            //    var users = new List<Usuario>();

            //    users.Add(new User { Id = 1, Username = "Admin", Password = "Admin", Role = "Administrador" });
            //    users.Add(new User { Id = 2, Username = "Usuario", Password = "Usuario", Role = "Default" });

            //    return users.Where(a => a.Username.ToUpper() == Username.ToUpper() && a.Password == Password).FirstOrDefault();

            //}
        }
    }
}
